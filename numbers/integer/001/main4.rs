// Fill the blanks to make it work
fn main() {
    // note here the i8::MAX represents the maximum number
    // possible represented by the i8 data type
    assert_eq!(i8::MAX, 127);
    assert_eq!(u8::MAX, 255);

    println!("Success!");
}
